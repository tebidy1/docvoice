import 'package:flutter/material.dart';

class MedColors {
  // Primary Brand Colors (Gulf Identity)
  static const primary = Color(0xFF00A6FB); // Cyan Blue
  static const primaryDark = Color(0xFF0086C8); // Darker Blue
  static const accent = Color(0xFF0086C8); // Matching accent
  
  // Semantic Colors
  static const success = Color(0xFF2ECC71); // Green 500
  static const warning = Color(0xFFFFC107); // Amber 500
  static const error = Color(0xFFEF4444); // Red 500
  
  // Text Colors
  static const textMain = Color(0xFFFFFFFF); // White
  static const textMuted = Color(0xFFB9D1E4); // Soft Blue-Grey
  static const textMicro = Color(0xFF64748B); // Slate 500 (Keep for subtle details)
  
  // Backgrounds & Surfaces
  static const background = Color(0xFF0F172A); // Deep Navy (Medical Dark)
  static const surface = Color(0xFF0B2C55); // Slightly lighter navy for cards
  static const divider = Color(0xFF1E3A5F); // Blue-ish divider
  
  // Overlays
  static const overlay = Color.fromRGBO(8, 28, 54, 0.85); // Navy Opacity
}
